-- NGOKAF TRANS — référence schéma (créé automatiquement via SQLAlchemy)
-- Base: ngokaf_trans

CREATE DATABASE IF NOT EXISTS ngokaf_trans
  CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE ngokaf_trans;

-- Les tables users, buses, seats, routes, tickets, ticket_cancellations,
-- luggage, notifications, audit_logs, login_logs, sequences, app_settings
-- sont créées au premier lancement par database/init_db.py
